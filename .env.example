# Copy to .env for local development (.env is gitignored).
# Your GA4 Measurement ID enables usage analytics. It is NOT secret — it is
# visible in the live page to anyone — but it is kept out of the committed
# source and injected at deploy time from the GitHub Actions variable GA4_ID.
GA4_ID=G-XXXXXXXXXX
